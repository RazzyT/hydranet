#/bin/sh

# this script automates the process of installing cjdns for debian-based distros
# firstly, it checks and installs dependancies
sudo apt-get install nodejs git build-essential python2.7
# then downloads the source code for cjdns
git clone https://github.com/cjdelisle/cjdns.git cjdns
cd cjdns
# then compiles
./do
# lastly, it generates a configuration file
./cjdroute --genconf >> cjdroute.conf
# with some basic instructions on how to set it up
echo ""
echo "Find a friend who is already in the Hyperboria/HydraNet network and ask them for their network credentials."
echo ""
echo "Make sure that you plug the creds into your cjdroute.conf file before you start it."
echo ""
echo "To start up cjdns: sudo ./cjdroute < cjdroute.conf"
exit
